# Redactions (four-quadrant-tests)

Host-specific paths and hostnames were replaced with placeholders after the runs; scalar results are unchanged; original file hashes are listed in REDACTIONS.json.

18 file(s) in this bundle were edited: absolute local filesystem paths were replaced with a `<host-path>` placeholder (keeping a trailing real filename where useful), and the two internal machine nicknames used to run these jobs were replaced with a generic `gpu-host` placeholder (or `gpu_host` inside snake_case fields). No numeric or scalar result, hash, timestamp, or verdict was changed. Embedded base64 image payloads were left untouched. See REDACTIONS.json for the exact list of files, per-pattern replacement counts, and before/after SHA-256 hashes.
