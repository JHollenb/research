# A Clocked Ledger: exact, time-aware memory for a frozen language model

Draft paper (`paper.md`) and hashed evidence (`evidence/`, verify with `shasum -a 256 -c SHA256SUMS` in that folder). §4.7 (LongMemEval with the reader held fixed) is open pending job-e39bfd6977e5; E5 and E7 are pending.
