# A Clocked Ledger: exact, time-aware memory for a frozen language model

Paper (`paper.md`) and hashed evidence (`evidence/`, verify with `shasum -a 256 -c SHA256SUMS` in that folder). E5 (512k–10M) is the only run still pending.
