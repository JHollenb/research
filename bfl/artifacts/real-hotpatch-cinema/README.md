# Real FLUX Hotpatch Cinema proof bundle

This bundle contains the canonical four-specimen replication of the real FLUX.2 Hotpatch Cinema experiment. It preserves the admitted replication report, execution receipt, capability vector, and four scene/subject galleries. The earlier single-specimen report is retained as the development precursor.

The replication report records `4/4` passing specimens, `40` logical batched suffixes, `20` exact scalar confirmations, exact rollback, hostile-donor specificity, and the early-versus-late timing separation. The copied JSON files are the evidence used by the standalone demo; the verifier checks the central gates and headline ranges.

Run `python verify.py` from this directory.
