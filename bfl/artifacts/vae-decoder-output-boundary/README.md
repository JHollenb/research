# VAE and decoder output boundary: evidence bundle

This bundle preserves the Small Decoder report and receipt, third-party VAE swap images and report, residual-merge substitution records, blind probe result, and narrative evidence. The artifacts distinguish high image compatibility from exact parity and from output connectivity.

The verifier checks the declared speed/parameter reduction, image metrics, exact-parity failure count, residual-shift effect, and 283-arm blind-probe result.

Run `python verify.py` from this directory.
