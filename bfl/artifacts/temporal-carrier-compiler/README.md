# Temporal carrier compiler proof bundle

This bundle contains the route × denoising-step report, receipt, four pair-disjoint held-out images, and the detailed BFL-next ledger. The images are retained as visual evidence for the selected late image-stream carrier and its two held-out pairs.

Run `python verify.py` from this directory. The verifier checks the selected coordinate, authority curve, held-out metrics, near-zero controls, and all four images.

