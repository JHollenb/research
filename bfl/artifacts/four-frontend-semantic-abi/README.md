# Four-frontend semantic ABI proof bundle

This bundle contains the raw cross-family report, execution receipt, analysis, and representative Qwen, SmolLM, Mamba, donor, and sham images. It documents the distinction between a typed conditioner boundary and a semantically interchangeable payload.

Run `python verify.py` from this directory. The verifier checks the recorded 72-coordinate coverage, donor-rescue values, compact-mask failure, and representative visual artifacts.

