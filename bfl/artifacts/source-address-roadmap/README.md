# SourceAddress and recipient-local SourceWrite proof bundle

This bundle preserves the latest SourceAddress development matrix and the latest self-debugging SourceWrite verification. It combines the moving-source Pythia panel, checkpoint-local lowering, natural-language Qwen family-local compilation, and the payload-first/address-second self-debugging follow-up.

The matrix is the current cross-family summary. The self-debugging verification is a separate receipt because it answers a different question: whether feedback can identify and repair the payload/consumer plane before spending the next bounded update on address delivery.

Run `python verify.py` from this directory.
