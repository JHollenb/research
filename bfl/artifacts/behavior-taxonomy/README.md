# Behavior taxonomy: evidence bundle

This bundle preserves the count confirmation and runtime certificates, typography discovery and confirmation records, status notes, and the narrative behavior report. The files keep discovery behavior separate from confirmation-family behavior.

The verifier checks the exact count curve, typography admission values, lexical gate asymmetry, semantic substitution split, and the demoted joint.4 counting hypothesis.

Run `python verify.py` from this directory.
