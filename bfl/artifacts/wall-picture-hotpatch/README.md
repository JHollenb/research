# Wall-picture hotpatch proof bundle

This bundle contains the admitted report, certificate, execution receipt, representative source/target/opposite/sham/cinema images, and the two local source ledgers describing the protected-object contract. The accepted claim is explicitly composite: route-state movement plus an exact protected picture-crop write.

Run `python verify.py` from this directory. The verifier checks certificate admission, the four-specimen result strings, exact protected-object evidence, receipts, and representative images.

