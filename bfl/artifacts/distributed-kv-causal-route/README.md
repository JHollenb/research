# Distributed K/V causal route: evidence bundle

This bundle preserves the H9 raw audit and its adversarial adjudication for the 20-site native color-binding intervention panel. The audit records the 96 paired instances, 1,152 rendered endpoints, native S4 margins, endpoint recoveries, controls, and the reverse-direction donor-color collision count.

The bundle verifier checks the evidence values that support the report's bounded claim. It does not promote the result to bilateral semantic specificity or portability beyond the declared Klein 4B assay.

Run `python verify.py` from this directory.
