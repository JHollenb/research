# Scene circuit certificate proof bundle

This bundle contains the route-level scene report, certificate, certificate text, execution receipt, source/target/dose/wrong-color/sham/ablation images, and the failure-replay ledger. It keeps scene sufficiency and subject-collateral claims separate.

Run `python verify.py` from this directory. The verifier checks the certified gates, two-seed evidence, exact replay receipt, and representative visual controls.

