# FLUX.2 Dev paged execution: evidence bundle

This bundle preserves the FLUX.2 Dev runtime configuration, the model-family runtime narrative, the earlier failure-boundary diagnosis, and the conditioner-to-pixels explanation. The passed runtime claim is supported by the lifecycle and paging values in the narrative records; the older diagnosis is explicitly retained as a failed earlier run and is not treated as proof of success.

The verifier checks the 112.805 GB artifact, 24.011B conditioner, 32.223B denoiser, 11/11 gates, 336.41-second completion, 12.7 GB swap evidence, and diagnostic-only four-step metrics.

Run `python verify.py` from this directory.
