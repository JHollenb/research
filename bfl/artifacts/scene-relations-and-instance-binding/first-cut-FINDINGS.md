# The old object pointer does not enter the numerical writer

The strongest result is a code-level and native-consumer agreement: two exact spectral instance reads select distinct camera addresses and distinct typed program fingerprints, yet the existing donor-free writer produces identical tensors at all 12 tested seed × step boundaries. The final left-target and right-target images are byte-identical in all three seeds. This follows the writer equation: its numerical operands are the parent, predicate kernel, row indices, and dose; the declared object ID is checked but does not change those operands.

The compact `adjust` carrier also fails to produce contact in all three distinct two-camera seed outputs. This is a portability boundary, not evidence that its historical one-instrument effect was absent. The shifted compact control also makes no contact, but its perturbation energy is not matched and cannot certify lexical address specificity.

Native left/right source prompts make coarse hand contact with the requested side in 6/6 images. Four of these six sources add a third hand; only the third seed has two hands in both native target images. Actual focus-ring manipulation and exact camera identity are not established. The prompt requested identical cameras, but the native camera appearances differ, so this is an ambiguity probe rather than the full identical-instance test.

## Target contrast

At `joint.3`, define D=(H_right-H_left)/2. Only 5.07–7.89% of D's squared norm lies in the three saved predicate slots across the 12 observed seed × step states; the remainder lies outside them. The shared action displacement A=(H_right+H_left)/2-H_neutral has much larger energy than D. These are representation diagnostics, not claims of semantic location or causal strength.

The two spectral addresses selected equal-norm opposite signs of D with the predicate slots removed. This changed native RGB: combined left/right MAD is 3.805, 5.597, and 6.929 on the 0–255 scale. It produced no visible contact in the six combined branches or six pointer-only branches. A nonzero pixel interaction is therefore not evidence of a bound relationship.

Working inference: the old object argument is metadata-only at the writer, and the portable predicate lacks sufficient action authority in this two-camera context. The new directional operand reaches the pixels, but a working action backbone is missing. The next cut retains the shared native action state before testing direction.

## Execution and limitations

One guarded mrun lease on Beast, one BF16 Klein 4B load, three text encodes, zero fresh reference encodes, 33 scalar 512px images, 132 transformer forwards, 75.59 worker seconds, 86.31 scheduler seconds, 9,086 MB peak VRAM, and 15,360.1 MB peak RSS. mrun selected a 20,000 MB RAM/13,000 MB VRAM reservation without a manual override. All three zero-dose images are exact and all immutable-parent closures passed.

Visual judgments were made by one unblinded model reviewer. No trained contact instrument, held-out discovery split, checkpoint replication, or instance-layout swap has been established. Raw donor tensors were ephemeral; their hashes, per-row energies, prompts, code, operator, receipts, and all images are retained. The new target contrast is donor-assisted. Fourier mask translation was calibrated only in a separate CPU mechanics check and was not used to manipulate native RGB in this job.

Mechanics status: valid. Instrument status: contact-only visual review with source anatomy defects. Trend status: target-invariant old writer; compact action inactive in this context; target complement changes pixels without contact. Terminal status: not established.
