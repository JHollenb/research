# Native-state route selector v6 proof bundle

This bundle contains the v6 selector report, analysis, portable package, execution receipt, representative green held-out images, and the local route-selector ledger. It preserves the safety result: calibrated support detection caused abstention on an unsupported family, not a claimed route improvement.

Run `python verify.py` from this directory. The verifier checks the 106-row panel, disjoint split contract, calibration radius, held-out abstention result, and representative outputs.

