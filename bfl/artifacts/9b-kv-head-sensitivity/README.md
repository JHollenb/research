# 9B and 9B-KV head sensitivity: evidence bundle

This bundle preserves the physical head-ablation report, the Klein 9B and 9B-KV variant records, and the model-family context. The evidence compares four addressed heads on one prompt pair and two seeds.

The verifier checks the 96.1081% rewrite context, stable ordering, and reported MAD values. It does not infer semantic head ownership.

Run `python verify.py` from this directory.
