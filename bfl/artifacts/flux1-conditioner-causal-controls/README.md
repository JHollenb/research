# FLUX.1 conditioner causal-control proof bundle

This bundle contains the canonical corrected FLUX.1/SmolLM result, artifact manifest, execution receipt, held-out causal-control contact sheet, representative native/adapted/zero/wrong-source images, and the failure-replay ledger. The raw result preserves the final control contract, while the ledger records the derived means used in the standalone report.

Run `python verify.py` from this directory. The verifier checks the four held-out pairs, separate native no-op contract, source-specific wrong-prompt references, held-out image metrics, and representative images.

